# Test fixtures

Deterministic command traces, malformed packet samples, and minimized
regressions belong here when their implementation milestones begin. Fixture
files must document their format, source or generation seed, and expected
result. Milestone 0 intentionally adds no behavioral fixture.
